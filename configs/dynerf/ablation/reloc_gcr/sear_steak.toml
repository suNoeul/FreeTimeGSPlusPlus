extends = "../../ftgs/sear_steak.toml"

# Relocation score-mode ablation (gcr): gcr_clone score
# Only train.relocation.score_mode is overridden; init and all other settings inherit
# from the shared ftgs base so init.pt can be reused across settings.
[train.relocation]
score_mode = "gcr_clone"
